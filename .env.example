# --- Required for AI voice training + generation ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Optional: auto-post to X (Twitter) ---
# Get from https://developer.x.com — OAuth 2.0 user context token with tweet.write + users.read scope
X_BEARER_TOKEN=

# --- Optional: auto-post to Meta Threads ---
# Get from https://developers.facebook.com/apps — Threads use case enabled, long-lived user token
THREADS_USER_ID=
THREADS_ACCESS_TOKEN=
