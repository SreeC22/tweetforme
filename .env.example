# Waitlist
WAITLIST_WEBHOOK_URL=           # Slack / Discord / Zapier webhook for signup notifications

# AI
ANTHROPIC_API_KEY=              # https://console.anthropic.com

# Publishing — X
X_BEARER_TOKEN=                 # OAuth 2.0 token (tweet.write + users.read)

# Publishing — Threads
THREADS_USER_ID=
THREADS_ACCESS_TOKEN=           # Long-lived Meta Graph API token
